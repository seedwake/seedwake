[project]
name = "seedwake"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.14"
dependencies = [
    "ollama>=0.6.1",
    "python-dotenv>=1.2.2",
    "pyyaml>=6.0.3",
]
